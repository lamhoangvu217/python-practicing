# Create a simple calculator program that takes two numbers and an operator (+, -, *, /) as input and performs the operation
a = input("Enter a:")
b = input("Enter b:")
operator = input("Enter operator:");
result = a + operator + b
print(eval(result))
